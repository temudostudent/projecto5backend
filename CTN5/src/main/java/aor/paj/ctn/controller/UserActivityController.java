package aor.paj.ctn.controller;
import aor.paj.ctn.util.SessionManager;
/*
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class UserActivityController {

    private SessionManager sessionManager; // Injete o SessionManager aqui

    @PostMapping("/atividade-usuario")
    public void receberAtividadeUsuario(@RequestBody AtividadeUsuario atividadeUsuario) {
        sessionManager.userActivity(atividadeUsuario.getUserId());
        // Registre a atividade do usuário com o SessionManager
    }

    // Classe para representar dados de atividade do usuário
    static class AtividadeUsuario {
        private String userId;

        public String getUserId() {
            return userId;
        }

        public void setUserId(String userId) {
            this.userId = userId;
        }
    }
}*/
